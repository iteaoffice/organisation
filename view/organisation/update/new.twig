{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-update-organisation")) %}

<h1>{{ translate("txt-update-organisation") }}</h1>
<div class="row">
    <div class="col-md-12">
        <p class="lead">{{ translate("txt-organisation-update-procedure-explanation") }}</p>
    </div>
</div>

{% include 'organisation/partial/form/update' %}
