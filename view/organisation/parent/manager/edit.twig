{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-parent")) %}

<h1>{{ translate("txt-edit-parent") }}</h1>

{% do form.prepare() %}
{{ form().openTag(form)|raw }}

{% set parentForm = form.get('organisation_entity_parententity') %}

{{ organisationformelement(parentForm.get('organisation')) }}
{{ contactformelement(parentForm.get('contact')) }}
{{ lbs5formelement(parentForm.get('id')) }}
{{ lbs5formelement(parentForm.get('type')) }}

<div class="row mb-3">
    <label class="col-sm-3 col-form-label">{{ translate("txt-program-doa") }}</label>

    <div class="col-sm-9">
        <p class="form-control-plaintext">{% for doa in parent.doa %}{{ parentDoaLink(doa,'view','name') }}{% if not loop.last %} &middot; {% endif %}{% else %}{{ translate("txt-no-doa-signed") }}{% endfor %}</p>
    </div>
</div>

{{ lbs5formelement(parentForm.get('memberType')) }}
{{ lbs5formelement(parentForm.get('epossMemberType')) }}
{{ lbs5formelement(parentForm.get('artemisiaMemberType')) }}
{{ lbs5formelement(parentForm.get('dateEnd')) }}
{{ lbs5formelement(form.get('csrf')) }}

<div class="row mb-3">
    <div class="offset-sm-3 col-sm-9">
        {{ lbs5formelement(form.get('submit')) }}
        {% if form.has('delete') %}
            {{ lbs5formelement(form.get('delete')) }}
        {% endif %}
        {{ lbs5formelement(form.get('cancel')) }}
    </div>
</div>

{{ form().closeTag()|raw }}
