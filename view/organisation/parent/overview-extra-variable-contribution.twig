{% do headTitle().append(translate("txt-overview-extra-variable-contribution-for-parent-%s-in-year-%s")|format(parent, year)) %}

<h1 class="display-4">{{ translate("txt-overview-extra-variable-contribution-for-parent-%s-in-year-%s")|format(parent, year) }} {{ parentLink(parent, 'overview-extra-variable-contribution-pdf','icon',null, year) }}</h1>

{{ overviewExtraVariableContribution(parent,year) }}

