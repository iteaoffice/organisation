{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-advisory-board")) %}
{% do headTitle().append(translate("txt-edit-city")) %}

<h1>{{ translate("txt-edit-city") }}</h1>

{% include 'organisation/partial/form/advisory-board/city' %}

