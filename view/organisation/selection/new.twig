{% do headTitle().append(translate("txt-organisation")) %}
{% do headTitle().append(translate("txt-new-selection")) %}

<h1>{{ translate("txt-new-selection") }}</h1>

{% include 'organisation/partial/form/selection' %}