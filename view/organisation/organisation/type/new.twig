{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-new-organisation-type")) %}

<h1>{{ translate("txt-new-organisation-type") }}</h1>

{% include 'organisation/partial/form/type' %}
