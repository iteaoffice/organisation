<h1>{{ translate("txt-organisation-list") }}</h1>

<div class="row">
    {% for organisation in organisations %}
    <div class="col-md-4">
        <h3>{{ organisationLink(organisation,'view','name') }}</h3>

        {{ organisationLogo(organisation, 30) }}

        <dl>
            <dt>{{ translate("txt-partners") }}</dt>
            <dd>[@todo]</dd>
            <dt>{{ translate("txt-projects") }}</dt>
            <dd>[@todo]</dd>
        </dl>
    </div>

    {% if loop.index is divisibleby(3) %}
</div>
<div class="row">
    {% endif %}

    {% endfor %}
</div>