{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-organisation-%s")|format(organisationService.organisation.displayName)) %}

<h1>{{ translate("txt-edit-organisation-%s")|format(organisationService.organisation.displayName) }} </h1>

{% include 'organisation/partial/form/organisation' %}

{{ organisationLink( null ,'list','button') }}