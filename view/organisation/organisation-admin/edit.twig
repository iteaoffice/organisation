{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-organisation-%s")|format(organisation)) %}

<h1 class="display-4">{{ translate("txt-edit-organisation-%s")|format(organisation) }}</h1>

{% include 'organisation/partial/form/organisation' %}

