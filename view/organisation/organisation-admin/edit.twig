{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-organisation-%s")|format(organisationService.organisation)) %}

<h1>{{ translate("txt-edit-organisation-%s")|format(organisationService.organisation) }} </h1>

{% include 'organisation/partial/form/organisation' %}

{{ organisationLink( null ,'list','button') }}